apiVersion: 1
providers:
  - name: client-dashboards
    folder: Clients
    type: file
    options:
      path: /var/lib/grafana/dashboards/clients
  - name: devnet-dashboards
    folder: Devnet
    type: file
    options:
      path: /var/lib/grafana/dashboards/devnet
